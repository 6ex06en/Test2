%h4 Мои карты
-if @map.errors
	-flash.each do |key, value|
		%div(class="alert-#{key}")=value
-if @maps
	%table
		%tr
			%th Название
			%th Действия
		=render @maps
=form_for @map do |f|
	%p=f.label :title, "Название карты:"
	%p=f.text_field :title
	%p=f.submit "Создать карту"